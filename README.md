maniacal-runaway
================
This program will search through processes with the ps command, drop the root processes into a separate file and return the rest to standard-out. Needs more filtering for process info. Either in python or bash.
